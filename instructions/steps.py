# cmd -> code .
# click trust
# terminal -> uv venv
# create a .env (holds ur key) and requirements.text in this folder
# in requirements.text -> copy paste what's there
# terminal -> uv pip install -r requirements.text
# google -> gemini + mistral API Key and save them in .env as variables     #this api is diff for everyone so u ill use venv othwise if u used real anyone can use it on ur account
# create a new app.py file and copy paste that
